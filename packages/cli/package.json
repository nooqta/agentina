{
  "name": "agentina",
  "version": "0.1.0",
  "description": "Agent collaboration across trust boundaries — pair two parties, grant scoped access, exchange A2A tasks. Self-hosted, auditable.",
  "type": "module",
  "bin": { "agentina": "./dist/cli.js" },
  "main": "./dist/cli.js",
  "files": ["dist"],
  "scripts": { "build": "tsup src/cli.ts --format esm --dts --clean" },
  "dependencies": {
    "@agentina-mesh/protocol": "workspace:*",
    "@agentina-mesh/peer": "workspace:*",
    "@agentina-mesh/grants": "workspace:*",
    "@agentina-mesh/node": "workspace:*"
  },
  "engines": { "node": ">=20" },
  "license": "MIT"
}
